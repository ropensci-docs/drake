# Process an imported data object **\[stable\]**

For internal use only. Not a user-side function.

## Usage

``` r
process_import(import, config)
```

## Arguments

- import:

  Character, name of an import to process

- config:

  [`drake_config()`](https://docs.ropensci.org/drake/reference/drake_config.md)
  object
