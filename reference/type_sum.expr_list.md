# Type summary printing **\[questioning\]**

Ensures `<expr>` is printed at the top of any `drake` plan column that
is a list of language objects (e.g. `plan$command`).

## Usage

``` r
type_sum.expr_list(x)
```

## Arguments

- x:

  List of language objects.
